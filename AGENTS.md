# Agent Instructions

## Git Workflow

- Commit each completed step as you work.
- Use a clear, descriptive git commit message for every commit.
- Push to `origin` after each commit so the latest committed work is backed up remotely.
- Do not include unrelated user changes in a commit unless explicitly asked.
